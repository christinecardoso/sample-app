module EnrollmentHelpers

  # def completed_elements(enrollment)
  #   enrollment.activities
  #     .select { |a| a.activity_type_cd == 3 }
  #     .map { |a| a.course_module_element }
  # end

  # def progress(enrollment)
  #   if total_course_elements_for(enrollment.course) > 0
  #     100 * completed_elements(enrollments).size / course.total_elements
  #   else
  #     0
  #   end
  # end

end
